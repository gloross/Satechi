{% include "object-current-params" %}

{% assign param_view = false %}

{% for param in current_param_keys %}
  {% if param == "view" %}
    {% assign param_view = current_param_values[forloop.index0] %}
    {% break %}
  {% endif %}
{% endfor %}
