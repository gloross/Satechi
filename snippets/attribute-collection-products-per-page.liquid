{% include "object-current-params" %}

{% assign products_per_page = settings.products_per_page %}

{% for key in current_param_keys %}
  {% assign key_decoded = key | url_decode %}

  {% if key_decoded == "limit" %}
    {% assign products_per_page = current_param_values[forloop.index0] | times: 1 %}
    {% break %}
  {% endif %}
{% endfor %}
