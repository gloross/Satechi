{%- comment -%}TODO: metafield instead.{%- endcomment -%}
{% assign product_related_handles = "" | split: "  " %}

{% for tag in product.tags %}
  {% if tag contains "Related: " %}
    {% assign handle = tag | split: "Related: " | last | split: "  " %}

    {% assign product_related_handles = product_related_handles | concat: handle %}
  {% endif %}
{% endfor %}
